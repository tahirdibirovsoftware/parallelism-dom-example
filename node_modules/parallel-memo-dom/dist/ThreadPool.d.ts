interface ThreadPoolOptions {
    size: number;
    enableCaching?: boolean;
}
export declare class ThreadPool {
    private size;
    private threads;
    constructor(options: ThreadPoolOptions);
    exec(fn: (...args: any[]) => any, ...args: any[]): Promise<any>;
}
export {};
